package com.harshavardhan.Collections;

import java.util.Vector;

public class VectorPrac1 {

	public static void main(String[] args) {
		//create an Vector v1
		Vector v1 = new Vector();
		System.out.println(v1.size());
		System.out.println(v1.capacity());
		
				//add elements and also at specified index
		
		v1.add(1);
		v1.add("h1");
		System.out.println(v1);
		System.out.println(v1.size());
		System.out.println(v1.capacity());
				//add another vector v2 into vector v1 using for loop
				//add another vector v3 into v1 using inbuilt method
				//retrieve elements more than one try
				//delete one element, more than 1, vector
				//verify one element, more than 1, vector are there or another
				//update/replace at specified index
				//

	}

}
